# NinjaTags

## Build

1. Place `HytaleServer.jar` in `libs/`.
2. Run `./gradlew build`.
3. Find the plugin jar in `build/libs/` (and `dist/` if the copy task runs).

## Requirements

- Java 25 (Gradle will download a toolchain if configured locally).
